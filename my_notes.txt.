# Escritura de Archivo de Texto
# Crear y escribir en el archivo my_notes.txt
with open('my_notes.txt', 'w') as file:
    # Escribe al menos tres líneas de notas personales en el archivo
    file.write("Nota 1: Recoger la ropa del tintorero.\n")
    file.write("Nota 2: Comprar leche y pan en el supermercado.\n")
    file.write("Nota 3: Llamar al doctor para programar cita.\n")

# Lectura de Archivo de Texto
# Abrir el archivo y leer su contenido línea por línea
with open('my_notes.txt', 'r') as file:
    # Lee el contenido del archivo línea por línea
    for line in file.readlines():
        # Muestra en la consola cada línea leída
        print(line.strip())  # strip() elimina los espacios en blanco al inicio y al final de la línea

# Cierre de Archivos
# Se cierra automáticamente al salir del bloque "with"
